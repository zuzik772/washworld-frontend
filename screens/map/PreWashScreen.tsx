import { StyleSheet, Text, View } from "react-native";
import React from "react";
import Layout from "../../components/Layout";

const PreWashScreen = () => {
  return (
    <Layout>
      <Text>PreWashScreen</Text>
    </Layout>
  );
};

export default PreWashScreen;

const styles = StyleSheet.create({});
